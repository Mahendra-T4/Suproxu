import 'dart:async';

import 'package:flutter/material.dart';


Future<void> autoLogoutUser(BuildContext context, bool mounted) async {
  // final AuthService authService = AuthService();

  // Timer.periodic(
  //   const Duration(seconds: 1),
  //   (timer) {
  //     if (authService.isValidUser) {
  //       if (mounted) {
  //         logoutUser(context);
  //       }
  //     }
  //   },
  // );
}
